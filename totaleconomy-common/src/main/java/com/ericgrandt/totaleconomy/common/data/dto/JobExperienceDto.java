package com.ericgrandt.totaleconomy.common.data.dto;

// TODO: Move this to the dto package instead of having it in data
public record JobExperienceDto(String id, String accountId, String jobId, int experience) {

}
