currency {
    dollar {
        currency-plural=Dollars
        currency-singular=Dollar
        default=true
        prefix-symbol=true
        startbalance="100"
        symbol="$"
        transferable=true
    }
}
database {
    enable=false
    password=""
    url="mysql://[IP]:[PORT]/[DATABASE]"
    user=""
}
features {
    jobs {
        enable=true
        notifications=true
        salary=true
    }
    moneycap {
        amount=10000000
        enable=false
    }
    shops {
        min-price=0
        max-price=1000000000
        chestshops {
            enable=true
            cost=1000
        }
    }
}
language=en
save-interval=30
